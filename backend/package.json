{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "type-check": "tsc --noEmit",
    "sam:build:dev": "sam build --config-env=dev",
    "sam:deploy:dev": "sam deploy --config-env=dev --no-confirm-changeset"
  },
  "type": "module",
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.30.2",
    "@aws-sdk/client-dynamodb": "^3.966.0",
    "@aws-sdk/client-secrets-manager": "^3.971.0",
    "@aws-sdk/lib-dynamodb": "^3.966.0",
    "@hono/zod-validator": "^0.7.6",
    "aws-jwt-verify": "^5.1.1",
    "openai": "^6.16.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "esbuild": "^0.27.2",
    "tsx": "^4.21.0"
  }
}
